/**
 * ⚠️ ARQUIVO OBSOLETO — pode ser apagado.
 *
 * O cliente Prisma passou para `@vortexis/db` na reestruturação em
 * monorepo (FASE 3). Nada no app importa daqui.
 *
 * Mantido apenas porque o ambiente de automação não conseguiu remover
 * o arquivo. Apague-o quando puder:
 *   Remove-Item apps\storefront\src\lib\db.ts
 */
export { db } from "@vortexis/db";
